This build ships as a single-page dashboard (see ../index.html, ../js/app.js).
This folder is left as a placeholder for splitting the workspace, chat, and
history views into separate component files if you migrate to a framework
(React/Vue/etc.) later.
